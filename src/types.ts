export interface socialState {
    socialMedia: string,
    url: string
}

export interface dbResponse {
    social_name: string,
    social_value: string,
}

export interface socialLink {
    social_name: string;
    social_value: string;
}